/**
 * @file
 * Spark behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.spark = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
